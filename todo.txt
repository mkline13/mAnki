REQUIRED:

- make local backup and merge with main branch
- add all the Tests + UI Tests


OH wait, why is it running the scene delegate during tests?


IF POSSIBLE:
troubleshoot flashCardService.delete tests